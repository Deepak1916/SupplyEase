<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Supplier Management</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f8f9fa;
        }
        .navbar {
            background-color: #0a5bb2e9;
        }
        .navbar-brand {
            color: #fff !important;
            font-weight: bold;
        }
        .btn-logout {
            background-color: #dc3545;
            color: white;
            font-weight: bold;
            border: none;
        }
        .btn-logout:hover {
            background-color: #c82333;
        }
        .form-container {
            background-color: white;
            padding: 30px;
            border-radius: 8px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        }
        .table-container {
            background-color: white;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        }
        .table th, .table td {
            vertical-align: middle;
            text-align: center;
        }
        .btn-warning {
            font-weight: bold;
        }
        .btn-danger {
            font-weight: bold;
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg">
        <div class="container">
          <h1 class="navbar-brand">Supplier Management</h1>
          <a href="{{ url_for('index') }}" class="btn btn-logout">Logout</a>
        </div>
    </nav>
    <div class="container mt-5">
        <div class="form-container">
            <form action="{{ url_for('manage_suppliers') }}" method="POST">
                <input type="hidden" name="id" value="{{ supplier.id if supplier else '' }}">
                <div class="mb-3">
                    <label for="name" class="form-label">Supplier Name</label>
                    <input type="text" class="form-control" name="name" id="name" placeholder="Enter supplier name" value="{{ supplier.name if supplier else '' }}" required>
                </div>
                <div class="mb-3">
                    <label for="contact" class="form-label">Supplier Contact</label>
                    <input type="text" class="form-control" name="contact" id="contact" placeholder="Enter supplier contact" value="{{ supplier.contact if supplier else '' }}" required>
                </div>
                <div class="mb-3">
                    <label for="supply" class="form-label">Supply Details</label>
                    <input type="text" class="form-control" name="supply" id="supply" placeholder="Enter supplier details" value="{{ supplier.supply if supplier else '' }}" required>
                </div>
                <button type="submit" class="btn btn-primary">{{ 'Update Supplier' if supplier else 'Add Supplier' }}</button>
            </form>
        </div>

        <h2 class="mt-5">Supplier List</h2>
        <div class="table-container mt-3">
            <table class="table table-striped table-hover">
                <thead class="table-dark">
                    <tr>
                        <th>ID</th>
                        <th>Name</th>
                        <th>Contact</th>
                        <th>Supply</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    {% for supplier in suppliers %}
                    <tr>
                        <td>{{ supplier.id }}</td>
                        <td>{{ supplier.name }}</td>
                        <td>{{ supplier.contact }}</td>
                        <td>{{ supplier.supply }}</td>
                        <td>
                            <a href="{{ url_for('edit_supplier', supplier_id=supplier.id) }}" class="btn btn-warning btn-sm">Edit</a>
                            <form action="{{ url_for('delete_supplier', supplier_id=supplier.id) }}" method="POST" class="d-inline">
                                <button type="submit" class="btn btn-danger btn-sm">Delete</button>
                            </form>
                        </td>
                    </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
</body>
</html>
